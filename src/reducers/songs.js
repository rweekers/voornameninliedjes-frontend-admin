const song = (state, action) => {
    // TODO Add switch statement
    return state;
  };
  
  const songs = (state = [], action) => {
    // TODO Add switch statement
    return state;
  };
  
  export default songs;