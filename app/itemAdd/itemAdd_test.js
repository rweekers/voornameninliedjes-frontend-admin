'use strict';

describe('myApp.itemAdd module', function() {

  beforeEach(module('myApp.itemAdd'));

  describe('itemAdd controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var itemAddCtrl = $controller('ItemAddCtrl');
      expect(itemAddCtrl).toBeDefined();
    }));

  });
});