'use strict';

describe('myApp.remarks module', function() {

  beforeEach(module('myApp.remarks'));

  describe('remarks controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var remarksCtrl = $controller('remarksCtrl');
      expect(remarksCtrl).toBeDefined();
    }));

  });
});