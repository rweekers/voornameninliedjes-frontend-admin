'use strict';

describe('myApp.songAdd module', function() {

  beforeEach(module('myApp.songAdd'));

  describe('songAdd controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var songAddCtrl = $controller('SongAddCtrl');
      expect(songAddCtrl).toBeDefined();
    }));

  });
});