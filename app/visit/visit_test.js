'use strict';

describe('myApp.visit module', function() {

  beforeEach(module('myApp.visit'));

  describe('visit controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var visitCtrl = $controller('VisitCtrl');
      expect(visitCtrl).toBeDefined();
    }));

  });
});