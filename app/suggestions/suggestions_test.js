'use strict';

describe('myApp.suggestions module', function() {

  beforeEach(module('myApp.suggestions'));

  describe('suggestions controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var suggestionsCtrl = $controller('suggestionsCtrl');
      expect(suggestionsCtrl).toBeDefined();
    }));

  });
});