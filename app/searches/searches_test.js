'use strict';

describe('myApp.searches module', function() {

  beforeEach(module('myApp.searches'));

  describe('searches controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var searchesCtrl = $controller('SearchesCtrl');
      expect(searchesCtrl).toBeDefined();
    }));

  });
});