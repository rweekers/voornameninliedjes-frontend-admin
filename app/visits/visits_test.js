'use strict';

describe('myApp.visits module', function() {

  beforeEach(module('myApp.visits'));

  describe('visits controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var visitsCtrl = $controller('VisitsCtrl');
      expect(visitsCtrl).toBeDefined();
    }));

  });
});